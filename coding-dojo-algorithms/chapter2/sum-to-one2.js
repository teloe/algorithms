var sumToOne = function(n) {
  var sum = 0;
      arr = [];
      n = n.toString();
      n = n.split('');
      newString = n;

  for (var i = 0; i < newString.length; i++) {
    arr.push(newString);
    console.log(arr)
    // console.log(newString);
  }

};

sumToOne(928);