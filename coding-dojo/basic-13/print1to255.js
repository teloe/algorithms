// Write a function that prints an array with all the numbers from 1 to 255.

function print1To255() {
  var arr = [];
  for ( var i = 1; i <= 255; i++ ) {
    arr.push(i);
  }
  console.log(arr);
}

print1To255();
